all:
  children:
    production:
      hosts:
        montreal-01:
          ansible_host: montreal-01
          evdb_become: true

# This inventory has no test group. Playbooks default to the disposable test target,
# so production requires both -e target=production and -e apply=yes.
