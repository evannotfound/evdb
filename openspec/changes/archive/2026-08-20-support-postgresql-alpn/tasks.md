## 1. PostgreSQL ALPN Support

- [x] 1.1 Generate the managed Traefik default ALPN list with `postgresql` and retained defaults, cover the changed output with focused tests, and run the repository checks.
