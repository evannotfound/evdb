## 1. Consistent interactive input

- [x] 1.1 Package prompt_toolkit and route terminal text, choices, confirmations, and secrets through editable validated prompts, preserving injected and plain paths; cover editing, defaults, masking, validation, and cancellation.
- [x] 1.2 Replace the home keyboard loop with toolkit-owned live rendering and verify status updates preserve typed input.

## 2. Distribution and guidance

- [x] 2.1 Update guided-input documentation and verify the standalone executable includes and loads the new input dependency.
