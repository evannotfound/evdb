host:
  restic_password: local-restic-password
  dns:
    CLOUDFLARE_DNS_API_TOKEN: local-dns-token
projects:
  app-test-01:
    postgres:
      password: local-postgres-password
    kv:
      password: local-kv-password
      http_token: local-http-token
