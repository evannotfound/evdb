host:
  id: test-01
  domain: storage.example.com
  data_root: relative/data
  backup:
    repos: {postgres: /tmp/pg, kv: /tmp/kv}
  routing:
    acme_email: ops@example.com
    dns_provider: testdns
projects: {}
